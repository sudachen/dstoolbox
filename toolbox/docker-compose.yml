version: '3'
services:
  jupyterlab:
    build: .
    restart: always
    ports:
      - "127.0.0.1:8888:8888"
    volumes:
      - ${HOME}/work:/home/jupyter/work
    environment:
      DATABASE_URL: postgresql+psycopg2://test:test@192.168.99.1/test
      GITHUB_ACCESS_TOKEN: NONE
